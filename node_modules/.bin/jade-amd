#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jade-amd/bin/jade-amd" "$@"
  ret=$?
else 
  node  "$basedir/../jade-amd/bin/jade-amd" "$@"
  ret=$?
fi
exit $ret
